import { IconMapperPipe } from './icon-mapper.pipe';

describe('IconMapperPipe', () => {
  it('create an instance', () => {
    const pipe = new IconMapperPipe();
    expect(pipe).toBeTruthy();
  });
});
